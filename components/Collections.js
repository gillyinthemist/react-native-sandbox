import { Text, TouchableOpacity, View } from 'react-native';
import styles from '../styles';

export default function Collections() {
  return (
    <View style={styles.container}>
      <Text>Collections view</Text>
    </View>
  );
}
